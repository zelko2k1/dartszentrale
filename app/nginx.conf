server {
    listen 80;
    server_name _;
    root /usr/share/nginx/html;

    # SPA: alle nicht gefundenen Pfade auf index.html zurückfallen lassen,
    # damit clientseitiges Routing + Reload auf Unterseiten funktionieren.
    location / {
        try_files $uri $uri/ /index.html;
    }
}
