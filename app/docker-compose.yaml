services:
  dartzentrale:
    build:
      context: ./app
      dockerfile: Dockerfile
    ports:
      - "8081:80"
    environment:
      - VITE_PB_URL=http://192.168.200.7:8090
    restart: unless-stopped
