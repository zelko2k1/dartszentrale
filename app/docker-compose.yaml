services:
  dartzentrale:
    build:
      context: .
      dockerfile: Dockerfile
      args:
        # ← auf die IP DEINES PocketBase-Servers anpassen (Build-Zeit, nicht Laufzeit!)
        - VITE_PB_URL=http://192.168.1.50:8090
    ports:
      - "8081:80"
    restart: unless-stopped
