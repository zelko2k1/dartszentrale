services:
  dartzentrale:
    build:
      context: .
      dockerfile: Dockerfile
      args:
        - VITE_PB_URL=http://192.168.200.7:8090
    ports:
      - "8081:80"
    restart: unless-stopped
